@model List<PetManagementSystem.Models.Staff>
@{
    Layout = "_AdminLayout";
}

<div class="card shadow-sm border-0">
    <div class="card-header bg-white d-flex justify-content-between align-items-center">
        <h5 class="mb-0">Danh sách staff</h5>
        <a class="btn btn-primary" asp-action="CreateStaff"><i class="bi bi-plus-circle me-1"></i>Tạo staff</a>
    </div>
    <div class="card-body">
        @if (TempData["Success"] is string success)
        {
            <div class="alert alert-success">@success</div>
        }
        <div class="table-responsive">
            <table class="table table-striped table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Username</th>
                        <th>Họ tên</th>
                        <th>Chức vụ</th>
                        <th>Ngày tuyển</th>
                        <th>Trạng thái</th>
                        <th class="text-end">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.User?.Username</td>
                            <td>@item.User?.FullName</td>
                            <td>@item.Position</td>
                            <td>@item.HireDate?.ToString("yyyy-MM-dd")</td>
                            <td><span class="badge @(item.Status == "active" ? "text-bg-success" : "text-bg-secondary")">@item.Status</span></td>
                            <td class="text-end">
                                <a class="btn btn-sm btn-outline-primary" asp-action="EditStaff" asp-route-id="@item.Id">Cập nhật</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
